import logo from './images/Logo.svg';

function Header() {
    return (
        <header>
            <img src={logo} alt="little lemon logo" />
        </header>
    );
}

export default Header;